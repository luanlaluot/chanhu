import { Container, Typography } from "@mui/material";
import React from "react";

const Profile = () => {
  return (
    <Container maxWidth="xl">
      <Typography variant="h4">Hi, Profile</Typography>
    </Container>
  );
};

export default Profile;
